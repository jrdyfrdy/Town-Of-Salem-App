{
  "name": "town-of-salem-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/index.js"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3"
  }
}
